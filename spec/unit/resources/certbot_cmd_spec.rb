require 'spec_helper'

describe 'certbot_cmd' do
  step_into :certbot_cmd

  it 'does stuff' do
    # expect(action_class.cmd).to eql 'foo'
    true
  end

  it 'should be able to test action_class, but having issues'
  it 'is mostly just a stub at this point...'
end
